{% extends 'base.html.twig' %}

{% block page_title 'Homepage' %}
{% block title 'Homepage' %}

{% block body %}
    <p>Welcome to TP4!</p>

    {#
     FIXME:
        Lister les utilisateurs mais pas les admins.
        L'utilisateur actuelement connecté doit être en gras.
        Au clique sur un utilisateur je suis redirigé sur sa page de profile
     #}

    <ul>
        {% for user in users %}
            {% if user == app.user %}
                <li>
                    <strong><a href="/profile/{{ user.id }}">{{ user.firstname }} {{ user.lastname }} - {{ user.email }}</a></strong>

                </li>
        {% else %}
            <a href="/profile/{{ user.id }}">{{ user.firstname }} {{ user.lastname }} - {{ user.email }}</a>
        {% endif %}
        {% endfor %}
    </ul>
{% endblock %}
